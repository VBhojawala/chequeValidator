CREATE TABLE ACCOUNT ( ACCOUNT_NUMBER INT AUTO_INCREMENT PRIMARY KEY, ACCOUNT_HOLDER_NAME VARCHAR(250) NOT NULL, BANK_NAME VARCHAR(50) NOT NULL, IFSC VARCHAR(50) NOT NULL, MICR VARCHAR(50) NULL, START_DATE DATE)